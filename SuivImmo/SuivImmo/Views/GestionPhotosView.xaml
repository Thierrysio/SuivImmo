<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SuivImmo.Views.GestionPhotosView">
    <ContentPage.Content>
        <StackLayout>
            <Grid RowDefinitions="*,90,*" ColumnDefinitions="*,90,*">
                <Ellipse
             Grid.Row="1"
                    Grid.Column="1"
            HorizontalOptions="Center"
            VerticalOptions="Center"
            WidthRequest="90"
            HeightRequest="90"
            Stroke="#FFFF9900"
            StrokeThickness="4"
            Fill="White"/>

            <Image
            Grid.Row="1"
                    Grid.Column="1"
            HorizontalOptions="Center"
            VerticalOptions="Center"
            WidthRequest="80"
            HeightRequest="80"
            
            Source="{Binding Image}">
                <Image.Clip>
                    <EllipseGeometry
                    Center="40,40"
                    RadiusX="40"
                    RadiusY="40"/>
                </Image.Clip>
            </Image>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>